<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
        <meta name="description" content="A layout example that shows off a blog page with a list of posts."/>
        <title>Login - NSPress</title>
        <link rel="stylesheet" href="https://cdn.bootcss.com/pure/0.6.0/pure-min.css"/>
        <!--[if lte IE 8]>
            <link rel="stylesheet" href="https://yui-s.yahooapis.com/pure/0.6.0/grids-responsive-old-ie-min.css">
        <![endif]-->
        <!--[if gt IE 8]>
            <!-->
            <link rel="stylesheet" href="https://cdn.bootcss.com/pure/0.6.0/grids-responsive-min.css">
        <!--<![endif]-->
        <link rel="stylesheet" href="/css/admin.css" />
    </head>
    
    <body>
        <div class="login">
            <form class="pure-form pure-form-stacked">
                        <legend id="login-title">NSPress</legend>
                        <fieldset class="pure-group">
                            <input id="email" class="pure-input-1" type="email" placeholder="Email">
                            <input id="password" class="pure-input-1" type="password" placeholder="Password">
                        </fieldset>
                        <button type="submit" class="pure-button pure-input-1 pure-button-primary">Sign in</button>

                        <label id="remember-label" for="remember" class="pure-checkbox">
                            <input id="remember" type="checkbox"> Remember me
                        </label>
                </form>
        </div>
    </body>
</html>